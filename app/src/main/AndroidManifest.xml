<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jiyoun.teampleboard_kjy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProjectMain" />
        <activity android:name=".SignIn" />
        <activity android:name=".FindId" />
        <activity android:name=".FindPw" />
        <activity android:name=".resetPW" />
        <activity android:name=".NewProject" />
        <activity android:name=".NewTodo" />
        <activity android:name=".KanbanTodo" />
        <activity android:name=".KanbanDoing" />
        <activity android:name=".KanbanDone"></activity>
    </application>

</manifest>